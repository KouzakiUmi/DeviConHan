# Tyrano 补丁工具箱：项目规范

本文说明设计边界与维护约定。函数参数、返回值和恢复阶段见 [TECHNICAL_REFERENCE.md](TECHNICAL_REFERENCE.md)，日常操作见 [README.md](README.md)。

## 设计原则

- 面向 Tyrano / Electron 游戏，`でびるコネクショん` 汉化补丁是内置示例。
- 安装采用文件覆盖：解包原始 ASAR，用 Patch 覆盖对应路径，再重新打包。允许补丁增加文件；没有自动删除原始文件或合并脚本内容的机制。
- 游戏数据异常时，优先引导用户通过 Steam 验证完整性。原版备份是可选恢复来源，可能过时；工具检查不等于官方文件认证或补丁兼容性证明。
- 文件写入、备份和替换是业务行为。通过校验、操作锁和恢复标记控制失败路径，不增加无关的全量复制或完整归档哈希读取。
- 存档单独备份和恢复。Steam 验证游戏文件不能替代存档备份。

## 模块职责

| 模块 | 职责 |
|---|---|
| `main.py` | 参数解析、日志与语言初始化、启动检查、GUI / 批处理入口 |
| `controllers/patch_controller.py` | 安装、补丁身份比较、本地还原、失败回滚与恢复标记 |
| `controllers/save_manager_controller.py` | 存档和备份扫描、操作结果转换 |
| `core/bootstrap.py` | 确定游戏路径，校验配置和游戏状态；GUI 可在异常状态下进入恢复界面 |
| `core/patcher.py` | 调用内部 ASAR 解包/打包实现 |
| `core/patch_info.py` | 补丁指纹与安装元数据读写 |
| `core/state_validator.py` / `core/steam.py` | 状态检查与基于关键文件哈希的更新判断 |
| `core/save_service.py` | 存档备份、还原、删除、迁移 |
| `core/config.py` | 用户配置、快照、热重载与持久化 |
| `core/fuse.py` | 独立的 Fuse 编辑及备份恢复 |
| `gui/main_window.py` / `gui/tabs/` | 页面、用户确认、后台操作和结果展示 |
| `utils/` | ASAR 读写、文件和路径操作、锁、日志、多语言、磁盘检查等 |

模块补充说明见 [MODULE_GUIDE.md](MODULE_GUIDE.md)。

## 补丁安装与状态

1. 定位所选 ZIP 的实际补丁根目录，或使用内置 Patch 目录。
2. 计算补丁源指纹，检查当前 ASAR、外置文件及安装记录。
3. 当前安装正常且指纹相同：跳过。指纹不同或旧记录缺少指纹：提示优先通过 Steam 恢复；GUI 可确认使用有效原版备份重新构建。
4. 解包选定的原始归档，覆盖补丁文件，再检查补丁源是否在过程中发生变化。
5. 在原归档旁生成 `.new` 和相应 `.unpacked`，校验后进入不可取消的提交阶段。
6. 首次安装通过同目录重命名创建 `.bak`，提交新归档及外置目录，写入元数据后清理临时文件。

`.patch_meta` 中的补丁源指纹用于判断是否选择了同一个包；`patch_files` 是配置中关键文件的实际哈希。两者用途不同，均不表示所有游戏文件已通过官方验证。

当前文件缺失或校验失败时，安装入口会停止。批处理不会替用户确认从旧原版备份更换补丁，也不会在状态不一致时自行丢弃备份。更细的 Steam 状态分支属于内部实现，以控制器入口的实际流程为准。

## 文件恢复与存档

- ASAR 与 `.unpacked` 是配套资源。还原前检查归档结构及已声明外置文件的存在性和大小。
- `restoring` 重试时，外置目录可能已经恢复；保留并校验已有目录，避免重复清理删除仅存文件。
- 准备阶段可取消，提交阶段不可安全取消。恢复失败时保留必要的恢复信息，并提示 Steam 验证完整性。
- 存档还原在同卷临时目录准备数据，再切换目录。结果区分成功、未修改或已回滚、回滚失败。
- 更改备份目录时，由用户选择是否迁移。复制并验证后才删除源备份；拒绝迁移后，本次会话仍展示旧位置的备份。

## 配置、语言与日志

用户配置固定写入 `~/.tyranopatcher/config.ini`，首次使用时从内置模板复制。游戏路径优先使用有效运行时路径；自动检测开启时优先扫描 Steam，失败后按配置和当前目录回退。

- 主界面只给出操作指引。
- 确认弹窗按“提问 → 操作及影响 → 建议”组织，先说明“是/否”实际执行什么。
- 面向用户的提示、返回消息和进度文本使用 `utils.language.T()`，同时补齐 `cn`、`en`、`jp`，变量使用命名占位符。
- 技术诊断日志使用英文；可复用的用户提示可同时写入日志。磁盘检查明细使用当前语言，不保留硬编码中文输出。
- 控制器已写日志时，GUI 回调用 `ui_log()` 只负责显示，避免重复记录。

## 开发者工具与维护

Fuse 编辑是独立操作，偏移由 `FUSE_WIRE_HEADER_LENGTH` 和 `FUSE_ASAR_INTEGRITY_OFFSET` 配置。仅在副本上确认目标游戏的 Fuse 布局后使用；修改配置不能保证兼容不同 Electron 版本。

后台任务通过 `AsyncOperationManager` 执行；取消使用 `_check_cancelled` 协作检查，不强行终止线程。界面工作线程通过调度回到主线程展示结果。

修改功能后运行相关回归测试，再运行完整测试；文案检查应核对三语键、占位符和用户可见输出。修改文档时同步三语 README、索引及模块指南，历史报告明确标注历史性质。不要把旧测试数量或未验证的性能数字作为当前保证。
